# Phase 4E-6: Goal History & Read-Only Archives Integration

## Integration Architecture
- `SelfJourneyScreen` deep-links directly to `GoalHistoryScreen` and `GoalHistoryDetailScreen`.
- Preserves single source of truth without duplicating historical entities.
